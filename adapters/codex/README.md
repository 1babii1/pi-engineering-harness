# Codex Adapter

Codex CLI reads `AGENTS.md` natively (repository root down to the working directory, nested files
override), so no extra file is installed. Keep detailed reusable knowledge in skills/references
rather than duplicating it into Codex-specific instructions. Keep `AGENTS.md` compact: agents read a
bounded amount of instruction text.
