# Verify

Use the `verification` skill. Detect the affected area from the diff and run the smallest sufficient deterministic checks. Produce a structured PASS / FAIL / NOT RUN report and classify failures.
