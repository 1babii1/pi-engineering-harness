Backend verification: NOT RUN (dotnet is not installed here). The change is unverified: no build or
test evidence exists for it. It needs a run on a machine with the .NET SDK before it can be called
verified.
