# Frontend Instructions

- Follow the established frontend feature and component structure.
- Keep server state in the project's server-state library; do not duplicate it into client state without a documented reason.
- Use effects only for synchronization with external systems.
- Preserve accessibility and existing design-system conventions.
- Verify frontend changes with the repository-specific commands in `.pi/project/commands.md`.
